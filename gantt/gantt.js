const $=selector=>document.querySelector(selector);let tasks=[];const example=[{id:'A',duration:3,predecessors:[]},{id:'B',duration:4,predecessors:['A']},{id:'C',duration:2,predecessors:['A']},{id:'D',duration:5,predecessors:['B']},{id:'E',duration:3,predecessors:['B','C']},{id:'F',duration:2,predecessors:['D','E']}];
function renderList(){ $('#taskList').innerHTML=tasks.length?`<table><thead><tr><th>Tarea</th><th>Dur.</th><th>Precedentes</th><th></th></tr></thead><tbody>${tasks.map(task=>`<tr><td><b>${task.id}</b></td><td>${task.duration}</td><td>${task.predecessors.join(', ')||'—'}</td><td><button data-remove="${task.id}" aria-label="Eliminar ${task.id}">×</button></td></tr>`).join('')}</tbody></table>`:'<p class="copy">Agrega tareas o genera un ejercicio.</p>'}
function calculate(){try{const result=ProjectNetwork.calculate(tasks);renderResult(result)}catch(error){alert(error.message)}}
function renderResult(result){const unit=42,width=Math.max(420,result.projectDuration*unit);$('#summary').innerHTML=`<article><span>Duración mínima</span><strong>${result.projectDuration}</strong><small>unidades de tiempo</small></article><article><span>Camino crítico</span><strong class="critical-text">${result.criticalPaths.map(path=>path.join(' → ')).join(' / ')||'—'}</strong><small>holgura cero</small></article>`;const ticks=Array.from({length:result.projectDuration+1},(_,time)=>`<span class="tick" style="left:${time*unit}px">${time}</span>`).join('');const mobile=`<div class="mobile-gantt" role="list" aria-label="Cronograma vertical">${result.tasks.map(task=>`<article class="mobile-gantt-task ${task.critical?'critical':''}" role="listitem"><span class="mobile-task-id">${task.id}</span><div><b>Tiempo ${task.es} → ${task.ef}</b><small>Duración: ${task.duration} · Holgura: ${task.slack}</small><small>Precedentes: ${task.predecessors.join(', ')||'Ninguno'}</small></div></article>`).join('')}</div>`;$('#ganttChart').innerHTML=`<div class="legend"><span><i></i>Tarea</span><span><i class="critical-key"></i>Crítica</span><span><i class="slack-key"></i>Holgura</span></div><div class="gantt desktop-gantt"><div class="gantt-ruler"><span></span><div class="ticks" style="width:${width}px">${ticks}</div></div>${result.tasks.map(task=>`<div class="gantt-row"><span class="gantt-label">${task.id}</span><div class="gantt-track" style="width:${width}px"><span class="gantt-bar ${task.critical?'critical':''}" style="left:${task.es*unit}px;width:${task.duration*unit}px">${task.duration}</span>${task.slack?`<span class="slack-line" style="left:${task.ef*unit}px;width:${task.slack*unit}px"></span>`:''}</div></div>`).join('')}</div>${mobile}`;$('#metrics').innerHTML=`<div class="table-scroll"><table class="metrics-table"><thead><tr><th>Tarea</th><th>IT</th><th>FT</th><th>ITa</th><th>FTa</th><th>Holgura</th></tr></thead><tbody>${result.tasks.map(task=>`<tr class="${task.critical?'critical-row':''}"><td><b>${task.id}</b></td><td>${task.es}</td><td>${task.ef}</td><td>${task.ls}</td><td>${task.lf}</td><td>${task.slack}</td></tr>`).join('')}</tbody></table></div>`}
$('#taskForm').addEventListener('submit',event=>{event.preventDefault();const id=$('#taskId').value.trim().toUpperCase(),duration=Number($('#duration').value),predecessors=$('#predecessors').value.split(',').map(value=>value.trim().toUpperCase()).filter(Boolean);if(!id||duration<=0){alert('Indica un identificador y una duración válida.');return}if(tasks.some(task=>task.id===id)){alert(`La tarea ${id} ya existe.`);return}tasks.push({id,duration,predecessors});event.target.reset();$('#duration').value=3;renderList()});$('#taskList').addEventListener('click',event=>{const button=event.target.closest('[data-remove]');if(button){tasks=tasks.filter(task=>task.id!==button.dataset.remove).map(task=>({...task,predecessors:task.predecessors.filter(pred=>pred!==button.dataset.remove)}));renderList()}});document.querySelectorAll('[data-count]').forEach(button=>button.addEventListener('click',()=>{tasks=ProjectNetwork.random(Number(button.dataset.count));renderList();calculate()}));$('#exampleBtn').addEventListener('click',()=>{tasks=structuredClone(example);renderList();calculate()});$('#calculateBtn').addEventListener('click',calculate);function setTheme(theme){const dark=theme==='dark';document.documentElement.dataset.theme=theme;$('#themeToggle').setAttribute('aria-checked',String(dark));$('.theme-label').textContent=dark?'Modo claro':'Modo oscuro';$('.theme-knob').textContent=dark?'☾':'☼';localStorage.setItem('chrismuladores-theme',theme)}$('#themeToggle').addEventListener('click',()=>setTheme(document.documentElement.dataset.theme==='dark'?'light':'dark'));setTheme(localStorage.getItem('chrismuladores-theme')||'dark');tasks=structuredClone(example);renderList();calculate();
